spring:
  datasource:
    # Uses Testcontainers to spin up PostgreSQL automatically for tests
    url: jdbc:tc:postgresql:16:///projet_indiv
    username: projet
    password: projet
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
